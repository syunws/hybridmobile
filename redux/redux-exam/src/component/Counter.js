import React, { Component } from 'react';

const Counter = (value) => {
    return (
        <div>
            <h1>{value}</h1>
            <button>+</button>
            <button>-</button>
        </div>
    );
}

